version: "2"

services:
  mongo:
    image: 'mongo:3.4.2'
    ports:
      - "27017"

  mike-test:
    image: 'omb:latest'
    command: "node bin/www"
    ports:
      - "3000:3000"
    links:
      - "mongo:mongo"

